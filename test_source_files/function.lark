fn addstatic[] <<
		fna <- 1000;
		fnb <- 500;
		return fna+fnb;
>>

fn add[a,b,c] <<
		return a+b+c;
>>

c <- 100;
fn addlocalvar[a, b] <<
		local c <- 6;
		return a+b+c;
>>

fnval <- addstatic();
sum <- add(1,2,3);
localsum <- addlocalvar(1, 2);
