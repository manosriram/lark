a <- (1+3)==2; // false
b <- true;
c <- -123;
e <- (1+2+3) + c + (3*1/10);
if (a==b) {
		c <- "ok";
} else {
		c <- "not_ok";
}
tt <- !false;
c <-> e;
a <-> c;

x <- 100;

fn add[x, y] <<
		local x <- 2;
		return x+y;
>>

fnval <- add(1,2);
