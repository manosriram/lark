a <- (1+3)==2; // false
b <- true;
c <- -123;
e <- (1+2+3) + c + (3*1/10);
if (a==b) {
		c <- "ok";
} else {
		c <- "not_ok";
}
tt <- !false;
c <-> e;
a <-> c;

x <- 100;

fn add[x, y] <<
return x+y;
>>

c <- 100;
fn addlocalvar[a, b] <<
		local c <- 6;
		return a+b+c;
>>

aa <- 100;
bb <- 200;
fnval <- add(aa, bb);
localsum <- addlocalvar(1, 2);
